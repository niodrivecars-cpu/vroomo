# Booking Knowledge — References

- `fleet/models.py` (Booking), `fleet/views.py` (booking_create).
- `domain/booking/` — rules, invariants, state machine, edge cases.
- `tests/performance/booking-workflow.js` — booking smoke + same-vehicle
  concurrency assertion.
- ADR 0005 — SQLite write-lock retry.
- `docs/load-testing.md` — run methodology.

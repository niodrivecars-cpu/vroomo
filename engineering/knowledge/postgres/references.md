# Postgres — References

- Postgres docs: https://www.postgresql.org/docs/
- `docker-compose.yml` — the `postgres:16` dev service.
- `config/settings/production.py` — DATABASE_URL wiring.
- `scripts/backup.sh`, `scripts/restore.sh` — backup/restore.
- `knowledge/hostinger/` — deployment environment.
- postgres MCP guide: `platform/mcp/postgres.md`.

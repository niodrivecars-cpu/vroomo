# Redis — References (planned)

- Populate when Redis is adopted. Start from Django cache + django-ratelimit docs:
  - https://docs.djangoproject.com/en/stable/topics/cache/
  - django-ratelimit backend options
